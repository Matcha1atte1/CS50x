#include <cs50.h>
#include <stdio.h>

int main (void)
{
    //prompt user for positive integer
    int n;
    do {
        n = get_int("Height: ");
    }
    while (n < 1);

    //print n height pyramid
    for (int i = 0; )


}

