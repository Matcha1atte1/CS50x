#include<stdio.h>
#include<cs50.h>
int main(void)
{
    // get scores
    int scores[3];
    for(i = 0; i < 3; i++)
    scores[i] = 

    //print average
    printf("Average: %f\n", (scores[0] + scores[1] + scores[2])/3.0);
}
